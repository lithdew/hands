# Hands validation — 20 September 2026

**All five requested cases have completed the Hands workflow and passed independent inspection, with minor advisories.** These counts describe bounded artifact workflows with checkpoint reuse and inspected outputs. They do not establish a fresh-task success rate or an end-to-end speedup.

- **Personal website — pass with advisories.** Two responsive pages, grounded personal/project copy, working local navigation, and desktop/mobile visual inspection. Final attempt `d7162030`: **146.083 s**; eight Jev decisions **3.487 s**; five specialist calls **137.364 s**. It reused a five-file bundle and one source record, then performed two repairs. Minor focus-outline and verbose-copy advisories remain.
- **HKUST calculus mock — pass with advisories.** An original 180-minute, 100-mark exam with 12 questions, a 40/60 Calculus I/II split, worked solutions and marking guides. Independent review checked every answer and a 23-page A4 render; syllabus years and past/sample papers are distinguished. Final attempt `f7db73c3`: **269.865 s**; six Jev decisions **3.098 s**; four specialist calls **249.959 s**. It reused 12 retrieved source records, generated the exam, then repaired it. Print density and typography can improve.
- **Frontier reinforcement learning — pass with advisories.** About 2,376 words, six themes, 13 primary works, contextual empirical/theory claims and three proposed experiments. Independent review checked actual source records, freshness, claim context and desktop/mobile output. Final attempt `2da3987e`: **195.775 s**; eight Jev decisions **3.499 s**; seven specialist calls **187.218 s**. It resumed seven files and 17 source records, correcting the Jev baseline description and provenance. The bounded discovery search was genuine but not exhaustive.
- **Matrix inversion — pass with advisories.** A 105-second silent Remotion video using actual ManimGL scenes, 12 keyboard-tested flashcards and a printable cheat sheet. Independent math, frame, print and card evidence was reused only after exact hash checks; mobile read-along, synchronization, scene selection and fullscreen were freshly tested. Final attempt `3f81c0aa`: **20.233 s**; four Jev decisions **1.490 s**; three specialist calls **9.475 s**. This was a preview-only resume of an existing encode. The earlier render took **149.746 s** including asset preparation and full decode; 20.233 s is not video-generation latency.
- **Hands pitch — pass with advisories.** A 75-second silent Remotion pitch with genuine focused website/calculus evidence, readable captions and working desktop/mobile playback, fullscreen and synchronized reading views. All 26 runtime checks and independent scene, transition, hash and claim checks passed. Final attempt `420d69a4`: **139.021 s**; six Jev decisions **2.764 s**; six specialist calls **45.755 s**. It resumed 13 files and repaired the evidence framing and caption placement. Portrait-inline fine text and the dense closing remain minor advisories. Earlier `e1906d3e` correctly stopped at `needs-review`; its **178.237 s** is excluded from accepted-attempt timing totals.

Across the five accepted final attempts, recorded Jev decisions total **32 calls / 14.338 s**. Specialist generation, repair and review total **25 calls / 629.771 s**; five routing calls add **4.564 s**. Selected attempt durations sum to **770.977 s**. Those sums exclude earlier attempts and reused work, and are not session wall time. Astra low handled most expressive work; Gemini handled the website route and two research image reviews. Source fetching, rendering and preview time are separate from model time.

**Email:** one authorized test send was verified in the existing browser's Sent folder. The saved final status is idle, without an agent error or pending approval. Recipient, subject, body, account identifiers and screenshots are excluded from committed evidence. The private verification stays under `out/win/`.

**Correct preview:** the live Hands panel audit selected the matching artifact frame, issued zero desktop-stream requests and loaded the 75-second video with no video error; fullscreen permission was present. The first audit caught a video CORS failure, and corrected reruns passed. Two navigation-abort request events remain recorded. This verifies artifact-panel selection in the observed states; it is not a general signed-in-browser streaming reliability result.

**Interruptions:** tests cover visible, grounded CAPTCHA handling through bounded ordinary UI actions, a two-submission budget, bounded polling and fresh observations. An integration test checks **Luna → Astra low → Luna** after the challenge clears, without another router call. No live CAPTCHA solve is claimed. Fresh native checks of interruption handling and saved browser selection passed **9 tests / 59 assertions**. The coordinating run also reported **143 WSL tests / 734 assertions**, **28 native workflow tests / 159 assertions**, six preview tests, **four media tests / 29 assertions** and a passing typecheck. The existing Chrome connection was reported ready after restart. These scopes are reported separately, not added into an invented full-suite count.

**Repair contract measurement:** four real calls used the same five-file context and one unique label change. Luna exact-edit/full-file took **1.108 / 6.934 s** with **30 / 1,066 output tokens**; Astra low took **1.846 / 8.803 s** with **30 / 1,033 tokens**. All outputs produced exactly the desired in-memory bytes, none truncated, and the original artifact was untouched. This is one sample per cell, run concurrently. Its compact `before`/`after` and `files` wrappers differ from production `BundlePatch`; it supports smaller repair responses, not an exact production latency promise. See [raw measurements and limitations](repair-contracts-2026-09-20.json).

**Production repair observed:** the accepted pitch run used the actual `BundlePatch` contract with **11 exact edits, zero full-file replacements, 2,092 output tokens and 20.406 s** from Astra low. Subsequent validation and independent inspection passed. This is one genuine workflow observation, separate from the controlled contract comparison; no before/after production speedup is inferred.

The [machine-readable report](hands-evals-2026-09-20.json) records complete run IDs, event-derived timings, private evidence paths and hashes. Raw artifacts, independent audits and screenshots remain under ignored `out/`. Minor advisories, source limits and checkpoint reuse remain visible; the generated outputs alone do not establish general autonomous computer-use reliability.
